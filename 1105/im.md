# 🧩 IM 시험 당일 오후 복습 노트  
**목표:** 오후 6시 시험 전까지 5개 핵심 유형 감각 되살리기  
**시간:** 약 4시간 (13:00~17:30)

---

## Ⅰ. 시뮬레이션 / 구현
**대표 문제:** SWEA 공 굴리기  
**핵심 패턴:** 방향배열 + 경계 체크 + 상태 갱신 순서  

```python
dx = [0, 0, 1, -1]
dy = [1, -1, 0, 0]

for i in range(N):
    for j in range(M):
        if board[i][j] == "O":  # 공 시작
            x, y = i, j
            while True:
                nx, ny = x + dx[d], y + dy[d]
                if 0 <= nx < N and 0 <= ny < M and board[nx][ny] != '#':
                    x, y = nx, ny
                else:
                    break
```

✅ **복습 포인트**  
- 이동 후 검사 vs 검사 후 이동 순서 구분  
- continue, break 구분  
- dx, dy 배열 익히기  

---

## Ⅱ. 정렬 + 조건 분기  
**대표 문제:** SWEA 당근 포장  
**핵심 패턴:** 정렬 → 경계 i, j 선택 → 조건 continue → `max-min` 최소화  

```python
carrots = sorted(map(int, input().split()))
ans = 10**8
possible = False

for i in range(1, N-1):
    if carrots[i-1] == carrots[i]:
        continue
    for j in range(i+1, N):
        if carrots[j-1] == carrots[j]:
            continue
        A, B, C = i, j-i, N-j
        if 0 in (A, B, C) or max(A, B, C) > N//2:
            continue
        diff = max(A, B, C) - min(A, B, C)
        ans = min(ans, diff)
        possible = True
print(f"#{tc} {ans if possible else -1}")
```

✅ **복습 포인트**  
- 불가능 조건을 continue로 필터링  
- ans=min(ans, diff) 패턴  
- 출력 포맷 주의 (#1 0)  

---

## Ⅲ. 누적합 / 슬라이딩 윈도우  
**대표 문제:** 백준 2559 수열  
**핵심 패턴:** 윈도우 이동 시 “맨 앞 빼고, 새 값 더함”

```python
N, K = map(int, input().split())
arr = list(map(int, input().split()))

window_sum = sum(arr[:K])
max_sum = window_sum

for i in range(K, N):
    window_sum += arr[i] - arr[i - K]
    max_sum = max(max_sum, window_sum)
print(max_sum)
```

✅ **복습 포인트**  
- 구간 이동 시 더하고 빼는 순서 고정  
- 첫 구간 sum(arr[:K]) 설정  
- N, K 입력 순서 혼동 주의  

---

## Ⅳ. 탐색 (BFS/DFS)  
**대표 문제:** 백준 2667 단지번호붙이기  
**핵심 패턴:** 방문 체크 + 범위 검사 + 재귀 or 큐  

```python
from collections import deque

def bfs(x, y):
    q = deque([(x, y)])
    visited[x][y] = 1
    cnt = 1
    while q:
        cx, cy = q.popleft()
        for dx, dy in [(1,0),(-1,0),(0,1),(0,-1)]:
            nx, ny = cx+dx, cy+dy
            if 0<=nx<N and 0<=ny<N and not visited[nx][ny] and grid[nx][ny]=='1':
                visited[nx][ny] = 1
                cnt += 1
                q.append((nx, ny))
    return cnt
```

✅ **복습 포인트**  
- visited는 enqueue 직전에 표시  
- 범위 조건 먼저 검사  
- DFS 시 재귀 깊이 제한 주의  

---

## Ⅴ. 그리디 / 규칙 수열  
**대표 문제:** 백준 13305 주유소  
**핵심 패턴:** 이전 상태 기준으로 현재 갱신  

```python
n = int(input())
dist = list(map(int, input().split()))
cost = list(map(int, input().split()))

min_cost = cost[0]
total = 0
for i in range(n - 1):
    if cost[i] < min_cost:
        min_cost = cost[i]
    total += min_cost * dist[i]
print(total)
```

✅ **복습 포인트**  
- min_so_far 패턴  
- 조건 만족 시 갱신, 아니면 누적  

---

## 🧠 시험 직전 체크리스트
✅ dx, dy 배열 방향 확실히 기억하기  
✅ continue와 break 차이 확실히 구분  
✅ 출력 형식 정확히 맞추기 (#1 포함)  
✅ 테스트케이스마다 변수 초기화  
✅ O(N²) 이하면 시간 내 충분  

---

## 🕒 제안 복습 타임라인
| 시간 | 내용 | 비고 |
|------|------|------|
| 13:00–14:00 | 시뮬레이션/구현 복습 | 공 굴리기·당근 포장 |
| 14:00–15:00 | 슬라이딩 윈도우·누적합 | 수열·주유소 |
| 15:00–16:00 | BFS/DFS 구조 복습 | 단지번호붙이기 |
| 16:00–17:00 | 전체 흐름 다시 읽기 | 코드 복기 |
| 17:00–17:30 | 손풀기·출력확인 | 간단 문제 1개 실행 |

---

📎 **Tip:**  
“조건을 빠르게 버리고 남은 경우만 처리한다.”  
이게 IM 난이도 문제의 정답률을 가르는 핵심입니다.
